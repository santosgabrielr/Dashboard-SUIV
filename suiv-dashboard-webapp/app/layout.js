export const metadata = {
  title: 'Dashboard SUIV - Login',
  description: 'Tela de Login',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
